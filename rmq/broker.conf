brokerClusterName = DefaultCluster
brokerName = broker-a
brokerId = 0
deleteWhen = 04
fileReservedTime = 48
brokerRole = ASYNC_MASTER
flushDiskType = ASYNC_FLUSH
# Address the broker advertises to NameServer. App (host) + dashboard (Docker) connect here.
# Use your machine LAN IP so BOTH work (ipconfig -> IPv4). Example below; change if your IP differs.
# host.docker.internal often fails for app-on-host on Windows (connect to host.docker.internal:10911 failed).
brokerIP1 = 192.168.1.83
autoCreateTopicEnable = true